---
title: "Development"
description: "Start and manage HTFlow development servers"
icon: "code"
---

## Overview

The HTFlow development server provides a local environment with hot reloading, automatic port management, and real-time preview of your Webflow-compatible project.

<Info>
Make sure you've [initialized a project](/htflow/commands/project-management) before starting the development server.
</Info>

## Start Development Server

Launch a local development server for your HTFlow project.

<Tabs>
  <Tab title="Extension UI">
    <Steps>
      <Step title="Open Your Project">
        Make sure you have an HTFlow project open in VS Code
      </Step>
      
      <Step title="Open HTFlow Panel">
        Press `Ctrl+Shift+H` / `Cmd+Shift+H` or click the HTFlow icon
      </Step>
      
      <Step title="Click Start Dev Server">
        Click the **"Start Development Server"** button
        
        <Check>The server will start automatically with smart port handling</Check>
      </Step>
      
      <Step title="View in Browser">
        Your default browser will open automatically to:
        ```text
        http://localhost:3000
        ```
        
        <Tip>The extension displays running servers in the HTFlow panel</Tip>
      </Step>
    </Steps>
  </Tab>
  
  <Tab title="Command Line">
    ```bash
    htflow dev
    ```
    
    Output example:
    ```text
    🚀 HTFlow Development Server
    
    ✓ Server running at http://localhost:3000
    ✓ Hot reloading enabled
    ✓ Press Ctrl+C to stop
    
    Ready in 1.2s
    ```
  </Tab>
</Tabs>

## Server Features

<CardGroup cols={2}>
  <Card title="Hot Reloading" icon="fire">
    Changes automatically refresh in the browser
  </Card>
  <Card title="Smart Port Handling" icon="network-wired">
    Automatic port detection and management
  </Card>
  <Card title="Error Overlay" icon="triangle-exclamation">
    Visual display of errors in the browser
  </Card>
  <Card title="Source Maps" icon="map">
    Debug with original source code
  </Card>
</CardGroup>

## Hot Reloading

HTFlow automatically reloads your changes:

<Tabs>
  <Tab title="HTML Changes">
    Edit any HTML file and see changes instantly:
    
    ```html index.html
    <div class="hero">
        <h1>Updated Title</h1>
        <!-- Changes appear immediately -->
    </div>
    ```
    
    <Check>Full page reload for HTML changes</Check>
  </Tab>
  
  <Tab title="CSS Changes">
    CSS updates without page reload:
    
    ```css styles/main.css
    .hero {
        background: #16A34A; /* Updates instantly */
    }
    ```
    
    <Check>Hot module replacement for CSS</Check>
  </Tab>
  
  <Tab title="JavaScript Changes">
    JavaScript updates with module replacement:
    
    ```javascript scripts/main.js
    console.log('Updated code'); // Runs immediately
    ```
    
    <Check>Smart reload preserves application state</Check>
  </Tab>
</Tabs>

## Port Management

HTFlow intelligently manages ports:

### Default Port

```javascript htflow.config.js
module.exports = {
  dev: {
    port: 3000,  // Default port
    host: "localhost"
  }
};
```

### Automatic Port Detection

If port 3000 is busy, HTFlow automatically finds the next available port:

```text Console Output
⚠️  Port 3000 is in use
✓  Using port 3001 instead
🚀 Server running at http://localhost:3001
```

### Running Multiple Servers

The extension tracks all running HTFlow servers:

<Frame>
  <img src="/images/checks-passed.png" alt="Running servers display" />
</Frame>

<Info>
You can run multiple HTFlow projects simultaneously on different ports
</Info>

## Server Status

Monitor your development server status in the HTFlow panel:

<AccordionGroup>
  <Accordion title="Server Running" icon="circle-check">
    ```text Status Display
    ✓ Development Server
    Port: 3000
    Status: Running
    Uptime: 15:23
    ```
  </Accordion>
  
  <Accordion title="Server Stopped" icon="circle-xmark">
    ```text Status Display
    ○ Development Server
    Status: Stopped
    Last run: 2 hours ago
    ```
  </Accordion>
  
  <Accordion title="Multiple Servers" icon="server">
    ```text Status Display
    ✓ Project A (Port 3000)
    ✓ Project B (Port 3001)
    ✓ Project C (Port 3002)
    ```
  </Accordion>
</AccordionGroup>

## Stop Development Server

Stop the running development server:

<Tabs>
  <Tab title="Extension UI">
    <Steps>
      <Step title="Open HTFlow Panel">
        Press `Ctrl+Shift+H` / `Cmd+Shift+H`
      </Step>
      
      <Step title="Click Stop Server">
        Click the **"Stop Server"** button next to the running server
      </Step>
      
      <Step title="Confirm">
        The server will stop and be removed from the running servers list
      </Step>
    </Steps>
  </Tab>
  
  <Tab title="Command Line">
    Press `Ctrl+C` in the terminal where the server is running
    
    ```text
    ^C
    ✓ Server stopped
    ✓ Cleanup complete
    ```
  </Tab>
</Tabs>

## Development Configuration

Customize your development environment:

```javascript htflow.config.js
module.exports = {
  dev: {
    // Server settings
    port: 3000,
    host: "localhost",
    
    // Auto-open browser
    open: true,
    
    // Hot reloading
    hot: true,
    
    // Live reload on errors
    liveReload: true,
    
    // Error overlay
    overlay: {
      warnings: true,
      errors: true
    },
    
    // Compression
    compress: true,
    
    // HTTPS (optional)
    https: false,
    
    // Proxy (optional)
    proxy: {
      "/api": "http://localhost:8080"
    }
  }
};
```

## Working with the Dev Server

### View Console Logs

Monitor browser console in VS Code:

<Tabs>
  <Tab title="VS Code Output">
    View server logs in the Output panel:
    - Select "HTFlow" from the output channel dropdown
    - See all server activity and errors
  </Tab>
  
  <Tab title="Browser DevTools">
    Use browser developer tools:
    - Press `F12` to open DevTools
    - View console, network, and debugging info
  </Tab>
</Tabs>

### Access from Other Devices

Test on mobile devices or other computers:

```text Network Access
Local:   http://localhost:3000
Network: http://192.168.1.100:3000
```

<Steps>
  <Step title="Find Your IP">
    Check the server output for the network URL
  </Step>
  
  <Step title="Configure Firewall">
    Allow incoming connections on the dev port
  </Step>
  
  <Step title="Access from Device">
    Open the network URL on your device's browser
  </Step>
</Steps>

## Performance Tips

<CardGroup cols={2}>
  <Card title="Minimize File Size" icon="compress">
    Keep files small for faster hot reloading
  </Card>
  <Card title="Optimize Images" icon="image">
    Use compressed images during development
  </Card>
  <Card title="Use Source Maps" icon="map">
    Enable for easier debugging
  </Card>
  <Card title="Close Unused Servers" icon="power-off">
    Stop servers you're not actively using
  </Card>
</CardGroup>

## Troubleshooting

<AccordionGroup>
  <Accordion title="Server Won't Start" icon="circle-xmark">
    **Common issues:**
    - Port already in use
    - Project not initialized
    - Missing dependencies
    
    **Solutions:**
    ```bash
    # Check if port is in use
    lsof -i :3000  # macOS/Linux
    netstat -ano | findstr :3000  # Windows
    
    # Install dependencies
    npm install
    
    # Reinitialize project
    htflow init
    ```
  </Accordion>
  
  <Accordion title="Hot Reload Not Working" icon="fire-flame-simple">
    **Fixes:**
    1. Check console for errors
    2. Clear browser cache
    3. Restart the development server
    4. Verify `hot: true` in config
    
    ```javascript
    // Ensure hot reloading is enabled
    dev: {
      hot: true,
      liveReload: true
    }
    ```
  </Accordion>
  
  <Accordion title="Can't Access from Network" icon="wifi">
    **Enable network access:**
    
    ```javascript
    dev: {
      host: "0.0.0.0",  // Listen on all interfaces
      port: 3000
    }
    ```
    
    <Warning>Only enable network access on trusted networks</Warning>
  </Accordion>
  
  <Accordion title="Browser Won't Open" icon="window">
    **Solutions:**
    1. Manually open the URL shown in the output
    2. Disable auto-open in config:
    
    ```javascript
    dev: {
      open: false
    }
    ```
    
    3. Check default browser settings
  </Accordion>
  
  <Accordion title="Performance Issues" icon="gauge">
    **Optimization steps:**
    - Reduce file sizes
    - Disable unnecessary features
    - Check for memory leaks
    - Update HTFlow CLI
    
    ```javascript
    dev: {
      overlay: false,  // Disable if causing issues
      compress: true   // Enable compression
    }
    ```
  </Accordion>
</AccordionGroup>

## Advanced Usage

### Custom Port

Specify a custom port:

```bash
htflow dev --port=8080
```

### HTTPS Development

Enable HTTPS for local development:

```javascript htflow.config.js
module.exports = {
  dev: {
    https: true,
    // Or with custom certificates
    https: {
      key: './certs/server.key',
      cert: './certs/server.crt'
    }
  }
};
```

### Environment Variables

Use environment-specific settings:

```javascript
module.exports = {
  dev: {
    port: process.env.PORT || 3000,
    host: process.env.HOST || "localhost"
  }
};
```

## Next Steps

<Card
  title="Validate Your Code"
  icon="shield-check"
  href="/htflow/commands/validation"
  horizontal
>
  Ensure your code meets Webflow compatibility standards
</Card>

<Card
  title="Build for Production"
  icon="hammer"
  href="/htflow/commands/build"
  horizontal
>
  Create production-ready builds
</Card>

