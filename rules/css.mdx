---
title: "CSS Rules"
description: "CSS best practices and styling guidelines for HTFlow-compatible development"
icon: "palette"
---

## Overview

HTFlow CSS rules ensure your styles are compatible with Webflow and Droip conversion processes. These rules focus on consistent styling, responsive design, and maintaining clean, maintainable CSS.

<Info>
  Follow these CSS rules to ensure seamless conversion and maintain design
  consistency across your website.
</Info>

## Core Principles

<CardGroup cols={2}>
  <Card title="Class-Based Styling" icon="tag">
    Use CSS classes, never IDs for styling
  </Card>
  <Card title="Consistent Containers" icon="box">
    Use single container classes across sections
  </Card>
  <Card title="Responsive Design" icon="mobile">
    Mobile-first approach with proper breakpoints
  </Card>
  <Card title="Modern Layout" icon="grid">
    Use CSS Grid and Flexbox for layouts
  </Card>
</CardGroup>

## 🚨 Critical CSS Rules

### ABSOLUTE PROHIBITIONS

❌ **NEVER use IDs for styling** (`#my-element`)  
❌ **NEVER use inline styles** (`style="color: red"`)  
❌ **NEVER use !important** unless absolutely necessary  
❌ **NEVER skip data-ht-styles** attribute on `<style>` tags  
❌ **NEVER use emojis** - use inline SVG icons only

### MANDATORY REQUIREMENTS

✅ **ALWAYS use CSS classes** for all styling  
✅ **ALWAYS use consistent container classes** across sections  
✅ **ALWAYS maintain consistent heading sizes** throughout the website  
✅ **ALWAYS use mobile-first responsive design**  
✅ **ALWAYS use semantic class names**

## Required CSS Structure

Every HTFlow project must include the `data-ht-styles` attribute:

```html
<style data-ht-styles>
  /* All CSS goes here */
</style>
```

## Container System

### Single Container Pattern

Use consistent container classes across all sections:

```css
/* Main container */
.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 20px;
}

.container-large {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 20px;
}

.container-small {
  max-width: 800px;
  margin: 0 auto;
  padding: 0 20px;
}
```

### Usage in HTML

```html
<div class="htflow-wrapper">
  <section class="hero-section">
    <div class="container">
      <!-- Hero content -->
    </div>
  </section>

  <section class="features-section">
    <div class="container">
      <!-- Features content -->
    </div>
  </section>

  <section class="testimonials-section">
    <div class="container">
      <!-- Testimonials content -->
    </div>
  </section>
</div>
```

## Typography System

### Consistent Heading Sizes

Maintain consistent heading sizes across your entire website:

```css
/* Heading sizes - keep consistent across all pages */
.heading-1 {
  font-size: 3rem;
  font-weight: 700;
  line-height: 1.2;
  margin-bottom: 1.5rem;
}

.heading-2 {
  font-size: 2.5rem;
  font-weight: 600;
  line-height: 1.3;
  margin-bottom: 1.25rem;
}

.heading-3 {
  font-size: 2rem;
  font-weight: 600;
  line-height: 1.4;
  margin-bottom: 1rem;
}

.heading-4 {
  font-size: 1.5rem;
  font-weight: 500;
  line-height: 1.4;
  margin-bottom: 0.75rem;
}

/* Body text */
.body-text {
  font-size: 1rem;
  line-height: 1.6;
  margin-bottom: 1rem;
}

.body-text-large {
  font-size: 1.125rem;
  line-height: 1.6;
  margin-bottom: 1rem;
}
```

### Usage Examples

```html
<h1 class="heading-1">Main Page Title</h1>
<h2 class="heading-2">Section Title</h2>
<h3 class="heading-3">Subsection Title</h3>
<p class="body-text">Regular paragraph text</p>
<p class="body-text-large">Large paragraph text</p>
```

## Layout Systems

### CSS Grid Layouts

```css
/* Grid layouts */
.grid-2 {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 2rem;
}

.grid-3 {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
}

.grid-4 {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1.5rem;
}

/* Responsive grid */
.grid-responsive {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
}
```

### Flexbox Layouts

```css
/* Flexbox layouts */
.flex {
  display: flex;
}

.flex-center {
  display: flex;
  align-items: center;
  justify-content: center;
}

.flex-between {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.flex-column {
  display: flex;
  flex-direction: column;
}

.flex-wrap {
  display: flex;
  flex-wrap: wrap;
}
```

## Component Styling

### Button Components

```css
/* Button base styles */
.button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0.75rem 1.5rem;
  border: none;
  border-radius: 0.5rem;
  font-weight: 500;
  text-decoration: none;
  cursor: pointer;
  transition: all 0.2s ease;
}

.button-primary {
  background-color: #16a34a;
  color: white;
}

.button-primary:hover {
  background-color: #15803d;
}

.button-secondary {
  background-color: transparent;
  color: #16a34a;
  border: 2px solid #16a34a;
}

.button-secondary:hover {
  background-color: #16a34a;
  color: white;
}

.button-large {
  padding: 1rem 2rem;
  font-size: 1.125rem;
}

.button-small {
  padding: 0.5rem 1rem;
  font-size: 0.875rem;
}
```

### Card Components

```css
/* Card components */
.card {
  background: white;
  border-radius: 0.75rem;
  padding: 2rem;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.card:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 25px -3px rgba(0, 0, 0, 0.1);
}

.card-header {
  margin-bottom: 1.5rem;
}

.card-title {
  font-size: 1.5rem;
  font-weight: 600;
  margin-bottom: 0.5rem;
}

.card-content {
  color: #6b7280;
  line-height: 1.6;
}
```

## Responsive Design

### Mobile-First Approach

Always start with mobile styles and add larger breakpoints:

```css
/* Mobile first (default) */
.hero-section {
  padding: 2rem 0;
  text-align: center;
}

.hero-title {
  font-size: 2rem;
  margin-bottom: 1rem;
}

/* Tablet */
@media (min-width: 768px) {
  .hero-section {
    padding: 4rem 0;
  }

  .hero-title {
    font-size: 3rem;
  }
}

/* Desktop */
@media (min-width: 1024px) {
  .hero-section {
    padding: 6rem 0;
  }

  .hero-title {
    font-size: 4rem;
  }
}

/* Large desktop */
@media (min-width: 1280px) {
  .hero-section {
    padding: 8rem 0;
  }
}
```

### Breakpoint System

```css
/* Standard breakpoints */
@media (min-width: 640px) {
  /* sm */
}
@media (min-width: 768px) {
  /* md */
}
@media (min-width: 1024px) {
  /* lg */
}
@media (min-width: 1280px) {
  /* xl */
}
@media (min-width: 1536px) {
  /* 2xl */
}
```

## Color System

### Consistent Color Palette

```css
/* Primary colors */
:root {
  --color-primary: #16a34a;
  --color-primary-light: #07c983;
  --color-primary-dark: #15803d;

  /* Neutral colors */
  --color-gray-50: #f9fafb;
  --color-gray-100: #f3f4f6;
  --color-gray-200: #e5e7eb;
  --color-gray-300: #d1d5db;
  --color-gray-400: #9ca3af;
  --color-gray-500: #6b7280;
  --color-gray-600: #4b5563;
  --color-gray-700: #374151;
  --color-gray-800: #1f2937;
  --color-gray-900: #111827;

  /* Semantic colors */
  --color-success: #10b981;
  --color-warning: #f59e0b;
  --color-error: #ef4444;
  --color-info: #3b82f6;
}
```

### Usage Examples

```css
.text-primary {
  color: var(--color-primary);
}

.bg-primary {
  background-color: var(--color-primary);
}

.text-gray-600 {
  color: var(--color-gray-600);
}

.bg-gray-50 {
  background-color: var(--color-gray-50);
}
```

## Spacing System

### Consistent Spacing Scale

```css
/* Spacing scale */
.spacing-xs {
  margin: 0.25rem;
}
.spacing-sm {
  margin: 0.5rem;
}
.spacing-md {
  margin: 1rem;
}
.spacing-lg {
  margin: 1.5rem;
}
.spacing-xl {
  margin: 2rem;
}
.spacing-2xl {
  margin: 3rem;
}
.spacing-3xl {
  margin: 4rem;
}

/* Padding scale */
.padding-xs {
  padding: 0.25rem;
}
.padding-sm {
  padding: 0.5rem;
}
.padding-md {
  padding: 1rem;
}
.padding-lg {
  padding: 1.5rem;
}
.padding-xl {
  padding: 2rem;
}
.padding-2xl {
  padding: 3rem;
}
.padding-3xl {
  padding: 4rem;
}
```

## Animation and Transitions

### Smooth Transitions

```css
/* Transition utilities */
.transition {
  transition: all 0.2s ease;
}

.transition-fast {
  transition: all 0.1s ease;
}

.transition-slow {
  transition: all 0.3s ease;
}

/* Hover effects */
.hover-lift:hover {
  transform: translateY(-2px);
}

.hover-scale:hover {
  transform: scale(1.05);
}

.hover-fade:hover {
  opacity: 0.8;
}
```

## Common Mistakes to Avoid

<Warning>
**Never do these things:**

- Use IDs for styling (`#my-element`)
- Use inline styles (`style="color: red"`)
- Use `!important` unless absolutely necessary
- Skip the `data-ht-styles` attribute
- Use different container classes in different sections
- Use inconsistent heading sizes across pages
- Forget responsive design (mobile-first)
- Use emojis instead of SVG icons
- Use `float` for layouts (use Grid/Flexbox)
- Skip proper spacing systems
- Use hardcoded colors instead of CSS variables
- Forget to test on different screen sizes
  </Warning>

## CSS Organization

### File Structure

```css
/* 1. Reset/Normalize */
/* 2. CSS Variables */
:root {
  /* ... */
}

/* 3. Base styles */
body {
  /* ... */
}

/* 4. Typography */
.heading-1 {
  /* ... */
}

/* 5. Layout */
.container {
  /* ... */
}

/* 6. Components */
.button {
  /* ... */
}

/* 7. Utilities */
.spacing-md {
  /* ... */
}

/* 8. Responsive */
@media (min-width: 768px) {
  /* ... */
}
```

## Validation

Use the HTFlow audit command to validate your CSS:

```bash
npx htflow audit
```

This will check your CSS against all HTFlow rules and provide detailed feedback on any violations.

## Next Steps

<CardGroup cols={2}>
  <Card title="JavaScript Rules" icon="code-branch" href="/rules/javascript">
    Learn JavaScript guidelines for HTFlow
  </Card>
  <Card title="Best Practices" icon="star" href="/rules/best-practices">
    Advanced tips and optimization strategies
  </Card>
  <Card title="Examples" icon="book" href="/rules/examples">
    Real-world CSS examples and templates
  </Card>
  <Card title="HTML Rules" icon="code" href="/rules/html">
    Review HTML requirements and guidelines
  </Card>
</CardGroup>
